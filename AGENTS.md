# AI Agents 协作规范 (AGENTS.md)

为了保持项目的一致性与代码库的整洁，所有的 AI Agent 和自动化脚本在参与此项目的前端开发或依赖管理时，**必须严格遵守以下规范**：

## 1. 依赖管理

- **包管理工具约束**：本前端项目严格使用 `pnpm` 作为依赖包管理工具。
- **禁止行为**：
  - **严禁** 在该项目中执行 `npm install` 或 `yarn install`。
  - **严禁** 提交 `package-lock.json` 或 `yarn.lock` 文件。
  - 仅允许生成和维护 `pnpm-lock.yaml` 文件。
- **执行命令要求**：在安装任何新的 npm 包时，只能使用 `pnpm add <package>`，例如：
  ```bash
  pnpm add puppeteer pptxgenjs
  ```

## 2. 提交与忽略规则

- **勿提交依赖文件**：任何形式的 `node_modules/` 目录均不得纳入版本控制（已配置在 `.gitignore` 中）。
- **清理与规避**：如果在执行过程中产生了临时文件（如截屏产生的临时图片、生成的 `.pptx` 测试包等），务必确保这些产物不被提交到远程仓库，使用完成后要自动清理或在 `.gitignore` 中进行忽略声明。

请所有 AI 助手在执行任何任务前，务必先读取此规则。
